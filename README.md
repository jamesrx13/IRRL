## Start project
`cargo build`

## Create a new recording
`cargo run -- record <recording_name>`

## Run a some record
`cargo run -- replay <recording_name>`

`cargo run -- replay <recording_name> <recording_number>`
